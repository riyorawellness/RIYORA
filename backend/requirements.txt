# RIYORA WELLNESS — production requirements
# Every version below is published on public PyPI (checked Feb 2026).
# Ranges use "compatible release" (~=) so `pip install` succeeds without
# a lock file. Freeze with `pip freeze > requirements.lock.txt` post-deploy
# if you want exact pinning.

# ── Web framework ──
fastapi==0.115.0
uvicorn[standard]==0.32.0
starlette==0.38.6
python-multipart==0.0.12

# ── Data + validation ──
pydantic==2.9.2
pydantic-settings==2.5.2

# ── Database ──
motor==3.6.0
pymongo==4.9.1

# ── Auth / security ──
PyJWT==2.9.0
bcrypt==4.2.0

# ── HTTP + config ──
httpx==0.27.2
requests==2.32.3
python-dotenv==1.0.1

# ── Rate limiting ──
slowapi==0.1.9
limits==3.13.0

# ── Reports / files ──
reportlab==4.2.5
openpyxl==3.1.5
qrcode==7.4.2
pillow==10.4.0

# ── Payments (currently mocked, keeps import ok) ──
razorpay==1.4.2
setuptools==75.1.0  # razorpay imports pkg_resources

# ── Testing (kept in prod image for pytest runs on-box) ──
pytest==8.3.3
pytest-asyncio==0.24.0
